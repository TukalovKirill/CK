# ── PostgreSQL (хост-машина, доступ из контейнера) ──
DB_NAME=ck_db
DB_USER=kirill
DB_PASSWORD=your_password_here
DB_HOST=host.docker.internal
DB_PORT=5432

# ── Django ──
SECRET_KEY=change-me-in-production
DEBUG=1
ALLOWED_HOSTS=*,147.45.150.208

# ── CORS ──
CORS_ALLOWED_ORIGINS=http://147.45.150.208:8082

# ── Email ──
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@localhost

# ── Frontend URL (для ссылок в инвайтах) ──
FRONTEND_URL=http://147.45.150.208:8082

# ── Vite (frontend, при локальной разработке) ──
VITE_API_BASE=/api/
VITE_WS_BASE=ws://147.45.150.208:8082
